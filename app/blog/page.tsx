import { Metadata } from "next";

export const metadata: Metadata = {
  title: {
    absolute: "Blog"
  },
}

const BlogPage = () => {
  return (
    <div>
      hello blog
    </div>
  )
}

export default BlogPage;